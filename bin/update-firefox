#!/usr/bin/env bash
# shellcheck shell=bash

SCRIPT_PATH=$(realpath "$0")
DIR_PATH=$(dirname "$SCRIPT_PATH")
DOTFILES_PATH=$(dirname "$DIR_PATH")

# shellcheck source=libs/z/z.sh
source "${DOTFILES_PATH}/libs/z/z.sh"

_z firefox-developer-edition-installer

./script.sh build_and_install
