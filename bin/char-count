#!/usr/bin/env ruby
# frozen_string_literal: true

def char_count(str)
  str.strip.length
end

def char_count_each(items)
  items.each do |item|
    puts char_count item
  end
end

if ARGV.any?
  char_count_each ARGV.each
else
  char_count_each $stdin.readlines
end
