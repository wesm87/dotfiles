#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'

function __dotfiles_touchx() {
  touch "$@"
  chmod +x "$@"

  for file in "$@"; do
    if [[ -z $(cat "$file") ]]; then
      echo "#!/usr/bin/env bash" >"$file"
    fi
  done
}

__dotfiles_touchx "$@"
