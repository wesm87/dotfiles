#!/usr/bin/env ruby
# frozen_string_literal: true

def print_each(items)
  items.each do |item|
    print item.strip
  end
end

if ARGV.any?
  print_each ARGV
elsif $stdin.readlines.any?
  print_each $stdin.readlines
else
  print ''
end
